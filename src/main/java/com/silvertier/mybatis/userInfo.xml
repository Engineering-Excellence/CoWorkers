<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.silvertier.dto.CommentDTO">

	<insert id="insert" parameterType="userDTO">
		insert into userinfo (userid, accountid, accountpassword, username, registernumber, isgender, email)
		values (userinfo_userid_seq.nextval, #{accountid}, #{accountpassword}, #{username}, #{registernumber}, #{isgender}, #{email})
	</insert>

</mapper>